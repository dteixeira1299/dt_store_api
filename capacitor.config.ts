import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'dt_store_api',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
